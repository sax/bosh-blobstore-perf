#!/usr/bin/env bash

TARGET=<%= link('webdav').instances.first.address %>

apt-get -y install siege

mkdir -p /data

pushd /data > /dev/null

  siege -c <%= p('downloader.concurrency') %> -b -r <%= p('downloader.per_thread') %> http://${TARGET}/droplet.zip

popd > /dev/null
